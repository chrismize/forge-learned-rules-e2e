# Repository rules for coding agents

Rules an agent should read before changing this repository.

<!-- BEGIN:learned-testing-and-linting -->
# Test and lint commands

• Run tests with `npm test`
• Run linting with `npm run lint`

HUMAN NOTE: keep this line exactly as written, it was added by a person.
<!-- END:learned-testing-and-linting -->

<!-- BEGIN:learned-npm-commands-unavailable -->
# npm commands not yet available in repository

This repository does not have a `package.json` file yet, so `npm test` and `npm run lint` commands fail. The repository currently only contains documentation files (AGENTS.md and README.md). When documenting npm commands in README.md, note that the actual Node.js project structure has not been set up.
<!-- END:learned-npm-commands-unavailable -->
